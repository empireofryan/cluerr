<div class="container">
  <h3>Account for <%= current_user.email %></h3>
  <% if @subscription.active %>
    <h3>Current plans renews at <%= 
    Time.at(@stripe_subscription["current_period_end"]).strftime("%D") %>

    <%- # link_to "Cancel subscription", users_cancel_subscription_path -%>
  <% else %>
    <%= render 'form' %>
  <% end %>
</div>